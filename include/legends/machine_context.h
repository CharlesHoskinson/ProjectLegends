#pragma once
// Forwarding header: legends -> aibox unification
#include <aibox/machine_context.h>
namespace legends {
  using namespace aibox;
}
// aibox::compat is already accessible via legends:: (using namespace aibox pulls it in)
