# ThreadSanitizer suppressions for Project Legends
#
# Hygiene policy (R1 / ci-stabilize-mandatory-lanes design D1):
# - One entry per race family (or single common root cause).
# - Every entry MUST be preceded by a comment with a tracking issue URL.
# - No module-wide globs (e.g. no bare "race:engine" matching half the tree).
# - Deleting a suppression entry and closing its issue happen in the same PR.
# - New detections after enforcement land as a new entry + new issue — never
#   by re-adding allow_failure on the TSan CI leg.
#
# Format: https://github.com/google/sanitizers/wiki/ThreadSanitizerSuppressions
#
# History:
# - #38 race:g_active_instance removed (std::atomic). Re-prove green TSan on
#   synchronized SHA after any related change.
# - #39 CrashBreadcrumb: first seqlock attempt failed TSan (audit F013:
#   non-atomic payload memcpy is still a data race). Replaced with
#   std::mutex serialization in crash_breadcrumb.{h,cpp}. Confirm green
#   TSan on the fixing SHA before treating as closed.

# (no active race suppressions)
