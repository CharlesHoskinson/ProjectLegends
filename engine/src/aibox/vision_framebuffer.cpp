/**
 * @file vision_framebuffer.cpp
 * @brief Implementation of VGA framebuffer access for vision model integration.
 *
 * Most implementations are in the header file (inline). This file contains
 * only implementations that could not be inlined.
 */

#include "aibox/vision_framebuffer.h"

namespace aibox::vision {

// All implementations are inline in the header file.
// This file exists for potential future non-inline implementations.

}  // namespace aibox::vision
