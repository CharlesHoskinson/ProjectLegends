# OSV-Scanner baseline for Project Legends dependency-scan CI job.
#
# Hygiene: every ignore entry MUST link a tracking issue; delete the entry in
# the same PR that upgrades/removes the vulnerable component.
#
# FluidSynth: the vulnerable in-tree 1.1.6-noglib copy was REMOVED (#43).
# Optional LEGENDS_ENABLE_FLUIDSYNTH uses system/FetchContent v2.5.2+ only.
# No FluidSynth CVE ignores remain — an empty baseline is intentional.

# (no accepted vulnerabilities)
